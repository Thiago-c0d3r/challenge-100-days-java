#### PROBLEMA
até agora conseguimos reunir email e senha em listas inteligentes, mas a pergunta é "isso representa um usuário de vdd?" e a resposta é:
NÃO
pr usuario tem:
- email
- senha
- nome
- idade
- etc...

#### SOLUÇÃO
OBJETOS= eles são pacotes de dados(atributos) + comportamentos(métodos)

mas antes para criarmos objetos, precisamos de uma receita para eles, 
chamamos de classes, a classe é o molde, o objeto é oq é feito a partir desse molde

#### APLICANDO
hj vamos pegar o HashMap que estavamos trabalhando, e vou colocar nossa class usuários (vamos criá-la) na generics do hashMap
pra parar de guardar dados soltos e começar a guardar objetos



#### conceito chave de hj
**Transformamos dados em entidades 
hoje deixamos o sistema de login mais escalável**